---
phase: 02-vite-hmr-transport-and-browser-bridge
plan: 03
type: execute
wave: 3
depends_on:
    - 02-02
files_modified:
    - src/vite/hmr.ts
    - src/vite.ts
    - test/vite-hmr.test.ts
autonomous: true
requirements:
    - GATE-02
    - GATE-03
    - TRAN-04
    - TRAN-05
    - TRAN-06
    - TEST-03
    - TEST-04
must_haves:
    truths:
        - 'SSR-environment source edits forward relevant normalized source files to the client HMR channel.'
        - 'SSR non-source module changes use JS/TS/MDX source importer fallback and do not broadcast unrelated updates.'
        - 'With `hmr: false`, source updates trigger a Vite full reload and do not send custom Qwik HMR events.'
        - 'With `hmr: false`, bridge injection and dev segment self-accept code stay disabled by the existing option path.'
    artifacts:
        - path: 'src/vite/hmr.ts'
          provides: 'SSR-to-client forwarding and hmr:false full-reload fallback'
          exports: ['createViteHmr']
        - path: 'test/vite-hmr.test.ts'
          provides: 'SSR forwarding and disabled full-reload coverage'
    key_links:
        - from: 'src/vite/hmr.ts'
          to: 'server.environments.client.hot.send'
          via: 'SSR source forwarding'
          pattern: 'environments.client.hot.send'
        - from: 'src/vite/hmr.ts'
          to: 'environment.hot.send'
          via: 'hmr:false full reload for client updates'
          pattern: "type: 'full-reload'"
        - from: 'src/vite.ts'
          to: 'src/dev.ts'
          via: 'rolldownOptions.hmr propagation'
          pattern: 'hmr'
---

<objective>
Complete Phase 2 transport behavior with SSR-to-client forwarding and `hmr: false` full-reload fallback.

Purpose: Server-discovered Qwik source changes must still reach the browser HMR bridge, and opt-out users must get Vite full reload behavior without custom Qwik HMR paths.
Output: SSR forwarding and disabled-HMR fallback behavior in `src/vite/hmr.ts` with focused tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-vite-hmr-transport-and-browser-bridge/02-RESEARCH.md
@.planning/phases/02-vite-hmr-transport-and-browser-bridge/02-PATTERNS.md
@.planning/phases/01-dev-qrl-segment-core/01-VERIFICATION.md
@.planning/phases/02-vite-hmr-transport-and-browser-bridge/02-01-SUMMARY.md
@.planning/phases/02-vite-hmr-transport-and-browser-bridge/02-02-SUMMARY.md
@AGENTS.md
@src/vite.ts
@src/vite/hmr.ts
@src/dev.ts
@test/vite-hmr.test.ts

<interfaces>
Expected server object shape for SSR forwarding tests:
```typescript
server.environments.client.hot.send({ type: 'custom', event: 'qwik:hmr', data: { files, t } });
server.environments.client.hot.send({ type: 'full-reload' });
```

Disabled HMR option path:

```typescript
qwik({ hmr: false });
rolldownOptions.hmr === false;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: test SSR forwarding and disabled full reload behavior</name>
  <files>test/vite-hmr.test.ts</files>
  <read_first>
    - `test/vite-hmr.test.ts` after Plan 02-02.
    - `.planning/phases/02-vite-hmr-transport-and-browser-bridge/02-RESEARCH.md` lines 40-51, 222-244, and 287-312.
    - `.planning/REQUIREMENTS.md` lines 12-14, 31-34, and 47-48.
  </read_first>
  <behavior>
    - SSR environment hot updates send `qwik:hmr` through `server.environments.client.hot.send`, not through the SSR hot channel.
    - SSR non-source module changes forward only relevant JS/TS/MDX importer source files.
    - `qwik({ hmr: false })` sends `{ type: 'full-reload' }` for relevant source updates and sends no `qwik:hmr` custom events.
  </behavior>
  <action>Add RED tests for SSR forwarding and disabled fallback. Use `callConfigureServer` to provide a mocked Vite server with `environments.client.hot.send`. Call `callHotUpdate` with `this.environment.name === 'ssr'` and module/importer shapes covering direct source and non-source fallback. Add disabled-HMR tests for both client and SSR paths if the helper exposes both channels; assert no custom event sends occur and exactly one full reload is sent on the appropriate client channel.</action>
  <verify>
    <automated>pnpm test test/vite-hmr.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation for missing SSR forwarding and disabled full reload behavior.
    - TEST-03 and TEST-04 coverage is explicit in test names or assertions.
  </acceptance_criteria>
  <done>SSR forwarding and opt-out fallback expectations are locked by failing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement SSR forwarding and hmr:false fallback</name>
  <files>src/vite/hmr.ts, src/vite.ts</files>
  <read_first>
    - `src/vite/hmr.ts` after Plan 02-02.
    - `/Users/jacksm5pro/dev/open-source/qwik/packages/qwik-vite/src/plugins/vite.ts` lines around upstream `hotUpdate` for SSR-to-client forwarding.
    - `src/dev.ts` lines 30-32 and `src/rolldown.ts` lines 313-320 for existing `hmr: false` segment accept/optimizer mode gating.
  </read_first>
  <behavior>
    - SSR environment changes collect relevant normalized source/importer files and send custom `qwik:hmr` events to the client hot channel when HMR is enabled.
    - Both client and SSR relevant updates send full reload instead of custom events when `hmr: false`.
    - The existing `hmr: false` option continues disabling dev segment self-accept through `rolldownOptions`.
  </behavior>
  <action>Extend `src/vite/hmr.ts` so `configureServer(server)` stores only the Vite server reference needed by the Vite-specific helper. For `environment.name === 'ssr'`, collect relevant source/importer files using the same filtering as the client path, call `basePlugin.api.invalidateDevSegments(file, 'server')`, invalidate returned SSR graph modules if present, and forward non-empty payloads to `server.environments.client.hot.send({ type: 'custom', event: 'qwik:hmr', data: { files: [...files], t: ctx.timestamp } })`. When HMR is disabled, skip bridge/custom-event paths and send `{ type: 'full-reload' }` to the client hot channel for relevant updates. Ensure `src/vite.ts` delegates `configureServer` to the helper while still setting `rolldownOptions.devServer = server` for Phase 1 lazy parent transforms.</action>
  <verify>
    <automated>pnpm test test/vite-hmr.test.ts test/vite-plugin.test.ts test/rolldown-runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SSR forwarding tests pass and use the client hot channel per TRAN-04 and TEST-03.
    - Disabled fallback tests pass and verify no `qwik:hmr` custom event is sent per GATE-02, GATE-03, and TEST-04.
    - Existing Phase 1 runtime tests still pass, proving `hmr: false` self-accept gating remains intact.
  </acceptance_criteria>
  <done>SSR-discovered source changes reach the client channel, and disabled HMR produces clean Vite full reload fallback behavior.</done>
</task>

<task type="auto">
  <name>task 3: run Phase 2 focused regression gates</name>
  <files>src/vite/hmr.ts, src/vite.ts, src/client/hmr-bridge.ts, test/vite-hmr.test.ts, test/vite-plugin.test.ts, test/rolldown-runtime.test.ts</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 46-58 for Phase 2 success criteria.
    - `.planning/REQUIREMENTS.md` Phase 2 requirements and out-of-scope table.
    - `.planning/phases/01-dev-qrl-segment-core/01-VERIFICATION.md` behavioral spot-checks.
  </read_first>
  <action>Run the focused tests and perform code-search gates for Phase 2 boundaries. Confirm no production source manually injects `@vite/client`, no router/app defaults were added, and no raw Rolldown browser dev server path was introduced. Use filtered grep/rg gates that do not count comments in planning docs.</action>
  <verify>
    <automated>pnpm test test/vite-hmr.test.ts test/vite-plugin.test.ts test/rolldown-runtime.test.ts && ! rg "@vite/client|entry\.ssr|src/root|preview middleware|createServer" src</automated>
  </verify>
  <acceptance_criteria>
    - Focused test command passes.
    - The production source boundary search exits successfully with no forbidden matches in `src`.
    - No source production code adds router defaults, manual `@vite/client` injection, preview middleware, or raw Rolldown dev server HMR.
  </acceptance_criteria>
  <done>Phase 2 implementation satisfies transport, bridge, opt-out, and boundary preservation checks.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                      |
| ------------------------------------------- | -------------------------------------------------------------------------------- |
| SSR Vite environment → client HMR channel   | Server-side module graph discoveries are forwarded to browser-connected clients. |
| User plugin option → HMR transport behavior | `hmr: false` disables custom Qwik HMR behavior and selects full reload fallback. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                | Disposition | Mitigation Plan                                                                                                  |
| ---------- | ---------------------- | ------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| T-02-03-01 | Denial of Service      | SSR-to-client forwarding | mitigate    | Reuse source/importer filtering and send only relevant normalized files, preventing broad SSR module broadcasts. |
| T-02-03-02 | Tampering              | `hmr: false` fallback    | mitigate    | Gate all custom `qwik:hmr` sends on `options.hmr !== false`; tests assert disabled mode sends only full reload.  |
| T-02-03-03 | Information Disclosure | SSR file payload         | mitigate    | Strip query/hash and avoid forwarding non-source module URLs to the browser payload.                             |
| T-02-03-04 | Spoofing               | Vite server channel      | accept      | Vite dev server owns HMR client connections; Phase 2 does not add auth or network listeners.                     |

</threat_model>

<verification>
Run Phase 2 focused gates:

```bash
pnpm test test/vite-hmr.test.ts test/vite-plugin.test.ts test/rolldown-runtime.test.ts
```

Then confirm out-of-scope boundaries with a no-match production source search:

```bash
! rg "@vite/client|entry\.ssr|src/root|preview middleware|createServer" src
```

</verification>

<success_criteria>

- SSR hot updates forward relevant source files to the client HMR channel.
- `hmr: false` sends full reload and sends no custom Qwik HMR events.
- Existing Phase 1 dev segment HMR gating remains intact.
- Focused tests cover bridge injection/resolution/loading, SSR forwarding, `hmr:false` full reload, and browser bridge behavior.
- Out-of-scope boundaries remain preserved: no manual `@vite/client` injection, no router defaults, and no raw Rolldown dev server.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-vite-hmr-transport-and-browser-bridge/02-03-SUMMARY.md`.
</output>
