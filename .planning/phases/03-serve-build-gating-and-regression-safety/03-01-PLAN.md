---
phase: 03-serve-build-gating-and-regression-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/vite-hmr.test.ts
  - test/vite-plugin.test.ts
  - test/rolldown-runtime.test.ts
  - src/vite.ts
  - src/vite/hmr.ts
  - src/rolldown.ts
  - src/dev.ts
autonomous: true
requirements: [GATE-04, TEST-08]
must_haves:
  truths:
    - "Production Vite build mode does not inject the Qwik HMR bridge into HTML."
    - "Build, SSR, raw Rolldown, and library optimizer paths never select HMR mode."
    - "Generated segment accept code stays restricted to enabled dev HMR contexts."
  artifacts:
    - path: "test/vite-hmr.test.ts"
      provides: "Build-command negative coverage for Qwik HMR bridge injection"
      contains: "command: 'build'"
    - path: "test/vite-plugin.test.ts"
      provides: "Vite build/SSR/library optimizer mode regression coverage"
      contains: "mode: 'prod'"
    - path: "test/rolldown-runtime.test.ts"
      provides: "Raw Rolldown and library no dev-only segment code coverage"
      contains: "import.meta.hot.accept("
  key_links:
    - from: "src/vite.ts"
      to: "src/vite/hmr.ts"
      via: "createViteHmr enabled gate"
      pattern: "serve && options\.hmr !== false"
    - from: "src/rolldown.ts"
      to: "@qwik.dev/optimizer"
      via: "transformModules mode selection"
      pattern: "mode:.*hmr"
---

<objective>
Add focused regression tests proving HMR code paths are gated away from production and non-serve bundler contexts.

Purpose: Phase 2 introduced serve-mode HMR transport; this plan freezes the serve/build boundary so maintainers can ship it without dev-only bridge or segment code leaking into build, SSR, raw Rolldown, or library transforms.
Output: Strengthened Vitest coverage in existing hook/runtime test files, with source changes only when a new regression test fails and requires the smallest owner-file gating fix.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-serve-build-gating-and-regression-safety/03-RESEARCH.md
@.planning/phases/03-serve-build-gating-and-regression-safety/03-PATTERNS.md
@.planning/phases/02-vite-hmr-transport-and-browser-bridge/02-03-SUMMARY.md
@test/vite-hmr.test.ts
@test/vite-plugin.test.ts
@test/rolldown-runtime.test.ts
@src/vite.ts
@src/vite/hmr.ts
@src/rolldown.ts
@src/dev.ts

<interfaces>
<!-- Executor should use these contracts/patterns directly. -->

From Phase 3 research and patterns:

```typescript
const forbiddenHmrStrings = [
	'virtual:qwik-hmr-bridge',
	'qwik:hmr',
	'import.meta.hot.accept(',
	"CustomEvent('qHmr'",
	'document.__hmrT',
	'location.reload',
];
```

Vite build gate pattern:

```typescript
const plugin = getPlugin(qwik(), 'vite-plugin-qwik');
callConfigResolved(plugin, { command: 'build', root: '/workspace/app' });
expect(await callTransformIndexHtml(plugin, '<html></html>')).toBeUndefined();
```

Optimizer mode gate pattern:

```typescript
expect(optimizerMock.transformModules).toHaveBeenCalledWith(
	expect.objectContaining({ mode: 'prod' }),
);
```

Rolldown dev-only accept negative pattern:

```typescript
expect(code).not.toContain('import.meta.hot.accept(');
expect(code).not.toContain("typeof document !== 'undefined'");
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add production build HTML bridge non-injection gate</name>
  <files>test/vite-hmr.test.ts, src/vite.ts, src/vite/hmr.ts</files>
  <behavior>
    - `qwik()` configured with Vite `command: 'build'` returns `undefined` from `transformIndexHtml`.
    - The serialized transform result for build mode contains none of `virtual:qwik-hmr-bridge`, `qwik:hmr`, `qHmr`, `document.__hmrT`, or `location.reload`.
    - Existing serve-mode tests continue to prove the bridge is injected only for Vite serve with HMR enabled.
  </behavior>
  <action>Add a failing regression test to `test/vite-hmr.test.ts` using existing `getPlugin`, `callConfigResolved`, and `callTransformIndexHtml` helpers. The test must model Vite build mode (`command: 'build'`) and assert no bridge tag or bridge runtime strings are returned, satisfying GATE-04. If it fails, compare the relevant behavior against `/Users/jacksm5pro/dev/open-source/qwik/packages/qwik-vite/src/plugins` first, then fix only the owner gate in `src/vite.ts` or `src/vite/hmr.ts`; do not add raw Rolldown browser HMR, app/router defaults, or manual `@vite/client` injection.</action>
  <verify>
    <automated>pnpm test test/vite-hmr.test.ts</automated>
  </verify>
  <done>Build-mode `transformIndexHtml` has explicit negative coverage, serve/default injection coverage still passes, and no production build HTML path references the Qwik HMR bridge.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add build and library optimizer mode gates</name>
  <files>test/vite-plugin.test.ts, src/vite.ts, src/rolldown.ts</files>
  <behavior>
    - Vite client build transforms call the optimizer with `mode: 'prod'`, not `mode: 'hmr'`.
    - Vite SSR/server build transforms call the optimizer with the server/build mode currently used by the plugin, never `mode: 'hmr'`.
    - Library builds call the optimizer with `mode: 'lib'` and preserve host-owned library output settings.
  </behavior>
  <action>Extend `test/vite-plugin.test.ts` around the existing optimizer mock patterns to cover build command, SSR/server environment, and library environment mode selection for GATE-04 and TEST-08. Assert `transformModules` never receives `{ mode: 'hmr' }` in those contexts. If a test fails, compare the relevant behavior against `/Users/jacksm5pro/dev/open-source/qwik/packages/qwik-vite/src/plugins` first, then apply the smallest fix in `src/vite.ts` command/environment propagation or `src/rolldown.ts` optimizer mode selection; keep `src/vite.ts` as adapter wiring and `src/rolldown.ts` as the optimizer boundary.</action>
  <verify>
    <automated>pnpm test test/vite-plugin.test.ts</automated>
  </verify>
  <done>Build, SSR/server, and library optimizer tests prove production/library transforms do not use HMR mode and existing Vite config behavior remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: add raw Rolldown and segment accept leakage gates</name>
  <files>test/rolldown-runtime.test.ts, src/rolldown.ts, src/dev.ts</files>
  <behavior>
    - Raw `qwikClient()` production transforms do not append generated segment `import.meta.hot.accept(` code.
    - `qwikServer()` and `qwikLib()` transforms do not emit bridge/runtime-only strings such as `qwik:hmr`, `qHmr`, `document.__hmrT`, or `location.reload`.
    - Existing dev HMR enabled and `hmr: false` segment tests keep their current behavior.
  </behavior>
  <action>Extend `test/rolldown-runtime.test.ts` using its existing optimizer mock and segment load patterns to prove raw Rolldown production, server, and library paths do not emit dev-only HMR strings for GATE-04. If a test fails, compare the relevant behavior against `/Users/jacksm5pro/dev/open-source/qwik/packages/qwik-vite/src/plugins` first, then fix only `src/dev.ts` accept-code gating or `src/rolldown.ts` mode/dev option propagation. Do not add a raw Rolldown browser dev server or production/library HMR support.</action>
  <verify>
    <automated>pnpm test test/rolldown-runtime.test.ts</automated>
  </verify>
  <done>Raw Rolldown/runtime tests cover no-HMR-leakage behavior for production, server, and library contexts while preserving existing dev HMR behavior.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                         |
| --------------------------------------- | ----------------------------------------------------------------------------------- |
| Vite config → plugin hooks              | Build/serve command and environment values determine whether HMR code is reachable. |
| Optimizer output → production artifacts | Generated code crosses from optimizer transforms into shipped JS chunks.            |

## STRIDE Threat Register

| Threat ID  | Category               | Component                         | Disposition | Mitigation Plan                                                                                             |
| ---------- | ---------------------- | --------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------- |
| T-03-01-01 | Tampering              | `src/vite.ts` / `src/vite/hmr.ts` | mitigate    | Add build-command regression test asserting `transformIndexHtml` returns `undefined` and no bridge strings. |
| T-03-01-02 | Information Disclosure | `src/rolldown.ts` optimizer mode  | mitigate    | Assert production/server/library transforms never call optimizer with `mode: 'hmr'`.                        |
| T-03-01-03 | Elevation of Privilege | Test commands                     | accept      | Commands are static repo-local `pnpm test` invocations with no user-supplied shell fragments.               |

</threat_model>

<verification>
Run all focused gates for this plan:

<automated>pnpm test test/vite-hmr.test.ts test/vite-plugin.test.ts test/rolldown-runtime.test.ts</automated>
</verification>

<success_criteria>

- GATE-04 has focused unit coverage for Vite build HTML, optimizer mode selection, and raw Rolldown/library no-HMR leakage.
- TEST-08 has the unit-level portion of SSR/library regression coverage ready for fixture artifact checks in Plan 03.
- Source files are changed only if the new regression tests fail, and any fix stays in the owning gate file.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-serve-build-gating-and-regression-safety/03-01-SUMMARY.md`
</output>
