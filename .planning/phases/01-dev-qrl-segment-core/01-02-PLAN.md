---
phase: 01-dev-qrl-segment-core
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - test/rolldown-runtime.test.ts
  - src/dev.ts
  - src/rolldown.ts
autonomous: true
requirements:
  - SEGM-03
  - SEGM-04
  - SEGM-06
  - TEST-06
must_haves:
  truths:
    - "Client and SSR generated dev segment caches stay isolated for the same apparent segment path."
    - "Editing or invalidating a parent source removes all generated segment cache entries derived from that parent."
    - "Generated dev QRL URL normalization handles query strings, root-relative paths, absolute filesystem paths, and platform separator cases consistently."
  artifacts:
    - path: "test/rolldown-runtime.test.ts"
      provides: "Focused tests for environment isolation, invalidation, and normalization"
      contains: "invalidate"
    - path: "src/dev.ts"
      provides: "Parent-to-segment index, normalization boundary, and invalidation primitive"
      exports:
        - "createQwikDev"
    - path: "src/rolldown.ts"
      provides: "Plugin-facing invalidation access while retaining environment-scoped segment IDs"
      contains: "segmentId(currentEnvironment"
  key_links:
    - from: "src/dev.ts"
      to: "segments Map"
      via: "parent index deletes encoded segment IDs"
      pattern: "segments\.delete"
    - from: "src/rolldown.ts"
      to: "src/dev.ts"
      via: "dev.recordSegment(module, currentEnvironment) keeps parent mappings in sync"
      pattern: "dev\.recordSegment\(module, currentEnvironment\)"
    - from: "test/rolldown-runtime.test.ts"
      to: "src/dev.ts"
      via: "hook-level tests exercise resolve/load/transform behavior"
      pattern: "callResolveId|callLoad|callTransform"
---

<objective>
Complete the dev segment core by making cache identity environment-scoped, adding a parent invalidation primitive, and strengthening URL/source normalization coverage.

Purpose: HMR updates cannot be correct if client/server segment entries collide, stale parent-derived segments remain cached, or query/root/absolute path forms resolve inconsistently.
Output: Focused tests and implementation for environment isolation, parent-derived segment invalidation, and normalization.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-dev-qrl-segment-core/01-RESEARCH.md
@.planning/phases/01-dev-qrl-segment-core/01-PATTERNS.md
@.planning/phases/01-dev-qrl-segment-core/01-01-PLAN.md
@AGENTS.md
@src/dev.ts
@src/rolldown.ts
@test/rolldown-runtime.test.ts
@test/helpers.ts

<interfaces>
<!-- Plan 02 builds on Plan 01 outputs and must preserve these contracts. -->

Existing segment identity from `src/rolldown.ts`:

```typescript
const SEGMENT = '\0qwik:segment:';

function segmentId(environment: QwikEnvironment, path: string) {
	return `${SEGMENT}${environment}:${path}`;
}
```

Existing path normalization boundary from `src/dev.ts`:

```typescript
function pathname(id: string) {
	return parsePath(id).pathname;
}

function devSegmentPaths(path: string, root: string | undefined) {
	const paths = new Set([path, withLeadingSlash(path)]);
	const devPath = getDevPath(path, root);
	if (devPath) {
		paths.add(devPath);
	}
	return paths;
}
```

Invalidation behavior to implement simply:

```typescript
// record: parent key -> encoded segment ids
// invalidate: for matching environment + parent/source path, delete each encoded id from segments
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add isolation, invalidation, and normalization tests</name>
  <files>test/rolldown-runtime.test.ts</files>
  <read_first>
    - test/rolldown-runtime.test.ts
    - test/helpers.ts
    - src/dev.ts
    - src/rolldown.ts
    - .planning/phases/01-dev-qrl-segment-core/01-PATTERNS.md
    - /Users/jacksm5pro/dev/open-source/qwik/packages/qwik-vite/src/plugins/plugin.ts
  </read_first>
  <behavior>
    - Test 1: client and server transforms for `/src/shared.tsx_click_abc.js` return separate client/server code when loaded through their matching hook contexts.
    - Test 2: invalidating parent `/src/home.tsx` removes generated segment `/src/home.tsx_click_abc.js` so a subsequent load returns `null` or triggers the configured parent transform rather than stale code.
    - Test 3: resolve/load handles `/src/home.tsx_click_abc.js?v=123`, root-relative `/src/home.tsx_click_abc.js`, absolute `/workspace/app/src/home.tsx_click_abc.js`, and a backslash-containing `C:\\workspace\\app\\src\\home.tsx_click_abc.js` by normalizing to consistent segment identity.
  </behavior>
  <action>Add tests in `test/rolldown-runtime.test.ts` that use existing hook helpers only. For server environment tests, use `qwikServer({ dev: true })` or the Vite hook context pattern already present in tests if needed; assert encoded IDs differ by containing `\0qwik:segment:client:` versus `\0qwik:segment:server:`. For invalidation, call the new plugin-facing invalidation method only if exposed on the plugin object by task 2; otherwise call the dedicated dev helper through an exported testable function only if that export is kept internal to `src/dev.ts` and not added to `package.json`. Do not use Vite module graph invalidation in Phase 1; this plan only deletes plugin cache entries and proves stale segment code is not served.</action>
  <verify>
    <automated>pnpm test test/rolldown-runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "segment:client" test/rolldown-runtime.test.ts` shows an assertion for client segment identity.
    - `grep -n "segment:server" test/rolldown-runtime.test.ts` shows an assertion for server segment identity.
    - `grep -n "invalidate" test/rolldown-runtime.test.ts` shows a parent invalidation test.
    - `grep -n "v=123" test/rolldown-runtime.test.ts` shows query-string normalization coverage.
    - `grep -n "C:\\\\workspace" test/rolldown-runtime.test.ts` shows platform-separator normalization coverage.
    - `pnpm test test/rolldown-runtime.test.ts` passes after task 2 implementation is complete.
  </acceptance_criteria>
  <done>Focused tests prove SEGM-03, SEGM-04, SEGM-06, and TEST-06 behavior without relying on Phase 2 Vite hot-update transport.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement parent index, invalidation primitive, and normalization boundary</name>
  <files>src/dev.ts, src/rolldown.ts</files>
  <read_first>
    - src/dev.ts
    - src/rolldown.ts
    - test/rolldown-runtime.test.ts
    - .planning/phases/01-dev-qrl-segment-core/01-RESEARCH.md
    - .planning/phases/01-dev-qrl-segment-core/01-PATTERNS.md
    - AGENTS.md
  </read_first>
  <action>Update `src/dev.ts` to track parent-derived generated segment IDs by environment and normalized parent path. Use `parsePath(id).pathname` for query/hash stripping, `pathe` helpers for filesystem-style normalization, and a tiny slash-normalization boundary for backslash inputs. When `recordSegment(module, environment)` records aliases from `devSegmentPaths(module.path, root())`, also associate each encoded segment ID with the normalized parent source path derived from the segment filename prefix before `_<name>.js`. Add an invalidation method on the object returned by `createQwikDev`, named `invalidate(parent: string, environment?: QwikEnvironment)`, that deletes all matching encoded segment IDs from the shared `segments` map and returns the deleted IDs as `string[]`. Update `src/rolldown.ts` only as needed to expose this primitive to tests through a non-public plugin property or existing hook-accessible path; do not add a package export and do not touch `src/vite.ts` hot update transport in this phase.</action>
  <verify>
    <automated>pnpm test test/rolldown-runtime.test.ts</automated>
    <automated>pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "invalidate(parent" src/dev.ts` shows the invalidation method.
    - `grep -n "segments.delete" src/dev.ts` shows cache entries are actually removed.
    - `grep -n "parsePath" src/dev.ts` shows URL query/hash stripping remains centralized.
    - `grep -n "replace(/\\\\/g, '/'" src/dev.ts` or an equivalent named helper shows platform separator normalization at the boundary.
    - `grep -n "segmentId(environment" src/rolldown.ts` shows environment-scoped IDs remain in place.
    - `pnpm test test/rolldown-runtime.test.ts` passes.
    - `pnpm check` passes.
  </acceptance_criteria>
  <done>Client/server segment cache entries are isolated, parent invalidation removes all derived generated segment aliases, and normalization handles query, root-relative, absolute, and backslash path inputs through focused tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                          | Description                                                                                               |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| Browser dev module request → normalization helper | URL/path input with queries, absolute paths, or platform separators becomes a generated segment identity. |
| Parent source edit signal → segment cache         | A parent path controls which generated segment cache entries are removed.                                 |

## STRIDE Threat Register

| Threat ID | Category          | Component                     | Disposition | Mitigation Plan                                                                                                                                                     |
| --------- | ----------------- | ----------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-04   | Tampering         | `src/dev.ts` normalization    | mitigate    | Centralize query stripping through `parsePath()` and platform separator handling through a named helper with focused tests for query/root/absolute/backslash cases. |
| T-01-05   | Denial of Service | parent invalidation           | accept      | Invalidation runs over in-memory dev segment maps for local development only; no production request path is introduced.                                             |
| T-01-06   | Tampering         | environment-scoped cache keys | mitigate    | Preserve `\0qwik:segment:<environment>:<path>` IDs and add tests proving client/server entries for the same path do not collide.                                    |

</threat_model>

<verification>
Run `pnpm test test/rolldown-runtime.test.ts` and `pnpm check`. Inspect grep gates for `invalidate(parent`, `segments.delete`, `segment:client`, `segment:server`, `v=123`, and `C:\\workspace`.
</verification>

<success_criteria>
SEGM-03, SEGM-04, SEGM-06, and TEST-06 are satisfied when client/server cache entries are isolated, parent invalidation deletes all generated segment aliases derived from that parent, normalization handles required URL/path forms, and all focused tests plus type/lint checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dev-qrl-segment-core/01-dev-qrl-segment-core-02-SUMMARY.md`
</output>
